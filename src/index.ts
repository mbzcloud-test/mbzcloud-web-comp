// named export
export const greetings =
  'Hello world!, Greetings from mbzcloud-web-comp github package v1.6.0.';

// default export
const sum = (a: number, b: number) => {
  return a + b;
};

export default sum;
